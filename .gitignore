# DuckDB databases (large binary files)
*.duckdb
*.duckdb.wal

# Parquet data files (large, regenerable from scripts)
silver/
gold/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# 環境變數（含 API keys，絕不 commit）
.env

# 測試覆蓋率
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# OS
.DS_Store
Thumbs.db
# macOS resource forks（ExFAT 產生）
._*
# Office 臨時鎖檔
~$*

# Runtime logs / status（每次執行都會變，不進 git）
logs/*.log
logs/*_status.json

# 大型數據目錄（L3 原始數據，不進 git）
crc_visium_data/
results_ana/
data_ana/

# MCseg / MSseg 工具（獨立專案，暫不公開）
scripts/msseg/
msseg_docs/
analysis_msseg/
backend_msseg/

# Bulk RNA — 數據不進 git（L2 結果與 QC 報告體積大，以路徑記錄即可）
bulk_rna_data/
references/genome/

# References — Google Drive 備份即可，不進 git
references/
reports/
docs/academic_potential_analysis.md
docs/architecture_audit_report.md
*.pdf
*.docx

# uv lock file and venv symlink
uv.lock
.venv

# 分析產出圖檔（路徑記錄於 analysis_history.result_path，圖檔本身不進 git）
results/

# Backup 目錄（家目錄已存，避免誤建在專案內）
bio_db_backups/

# Temp / scratch files
file_info_input.md
*.bak

# Architecture diagram source files (keep compiled PNG in docs/images/, ignore raw sources)
# docs/images/*.png  ← intentionally tracked (documentation assets)

# MCP 設定（含本機絕對路徑，不跨機器）
.mcp.json

# IDE
.vscode/
.idea/

# 蛋白質體數據（L3 數據，不進 git）
proteome_data/

# 大型簡報二進位檔案
*.pptx

# 臨時備份檔案
*.bak

# 論文草稿與投稿材料（非專案本體）
docs/paper/
gigascience_reviewer_pack/

# 封存圖檔（舊版草圖）
docs/images/archive/

# 內部規劃與開發紀錄（非用戶需要）
docs/plans/
docs/logs/execution_trace.md
docs/references/
SESSION_RESUME.md

# Benchmark 過程產物（Nextflow / Snakemake）
.nextflow/
.nextflow_cache/
.nextflow_work/
.nextflow.log*
.snakemake/
work/
benchmark/

# scratch 臨時腳本
scratch/

